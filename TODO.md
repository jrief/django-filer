* Custom image model with own fields.
* Thumbor thumbnailer is a must. Can be hosted locally.
* Support for S3 and other storages.
* ACLs on files and folders.
* In the browser, we need a `FilerFolderField` to select a folder, for instance to fill
  the images of a carousel.
* Ordering of files in a folder matters.
* The List-View of files should have configurable extra fields wich should also be sortable.
